# Environment Variables

# Optional: Server's default OpenAI API key (for shared usage with rate limits)
# If not provided, users MUST supply their own API keys
OPENAI_API_KEY=your-openai-api-key-here

# Server port
PORT=3000

# Encryption key for securing user-provided API keys (auto-generated if not set)
# IMPORTANT: Keep this secret and never commit to Git
ENCRYPTION_KEY=
